import React from "react";

const ExitButton = () => {
  return (
    <div className="home_exit">
      <button
        type="button"
        className="home_exit_button"
      >
          <span>
              Salir
          </span>
        
      </button>
    </div>
  );
};

export default ExitButton;
